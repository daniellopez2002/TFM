name: Test Secrets

on:
  workflow_dispatch:  

jobs:
  verify:
    runs-on: [self-hosted, Windows, X64, Local-1]

    env:
      UE_PATH: ${{ secrets.UE_PATH }}
      PROJECT_PATH: ${{ secrets.PROJECT_PATH }}

    steps:
      - name: Print secrets (sanitized)
        run: |
          echo "✅ Runner: $env:COMPUTERNAME"
          echo "🔹 UE_PATH: $env:UE_PATH"
          echo "🔹 PROJECT_PATH: $env:PROJECT_PATH"
        shell: pwsh
